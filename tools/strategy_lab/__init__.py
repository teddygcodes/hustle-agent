"""Strategy Lab — rapid hypothesis prototyping for new strategies (Session 51).

On-demand bridge between Session 48 discovery-agent findings and production
scanners. User writes a tiny ``CandidateStrategy`` (~20 lines), runs the
driver, gets a markdown report telling them what would have happened on
historical universe + clv data.

LAB OUTPUTS ARE UPPER-BOUND HYPOTHETICALS. See ``README.md`` for limitations.
Read ``CLAUDE.md`` "Canonical Data Schema Reference" before any data-loader
query.
"""
